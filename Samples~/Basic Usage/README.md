# Gradient Texture Generator Basic Usage

Create a generator asset from `Assets/Create/Control Tools/Gradient Texture Generator`, edit the gradient and width, then use the generated texture sub-asset in materials, shaders, or other systems that need a 1D gradient lookup texture.
